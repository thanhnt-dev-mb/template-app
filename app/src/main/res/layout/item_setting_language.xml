<?xml version="1.0" encoding="utf-8"?>
<layout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools">
    <data>
        <import type="android.view.View" />
        <variable
            name="data"
            type="com.merryblue.baseapplication.coredata.model.LanguageModel" />
    </data>

    <androidx.constraintlayout.widget.ConstraintLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:paddingHorizontal="@dimen/dimen16">

        <com.google.android.material.imageview.ShapeableImageView
            android:id="@+id/flagImage"
            android:layout_width="@dimen/dimen28"
            android:layout_height="@dimen/dimen28"
            android:layout_marginVertical="16dp"
            android:layout_marginStart="8dp"
            app:layout_constraintBottom_toBottomOf="parent"
            app:layout_constraintStart_toStartOf="parent"
            app:layout_constraintTop_toTopOf="parent"
            android:adjustViewBounds="true"
            android:scaleType="centerCrop"
            app:shapeAppearanceOverlay="@style/ShapeCircle"
            app:circleImageRes="@{data.flags}" />

        <TextView
            android:id="@+id/languageTitle"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_marginStart="12dp"
            android:text="@{data.language}"
            android:textSize="@dimen/text_size14"
            android:textColor="@color/toolbarTitleColor"
            tools:text="English"
            app:layout_constraintBottom_toBottomOf="parent"
            app:layout_constraintStart_toEndOf="@+id/flagImage"
            app:layout_constraintTop_toTopOf="parent" />

        <ImageView
            android:id="@+id/rightImage"
            android:layout_width="@dimen/dimen24"
            android:layout_height="@dimen/dimen24"
            android:src="@{data.selected ? @drawable/ic_check : null }"
            app:layout_constraintBottom_toBottomOf="parent"
            app:layout_constraintEnd_toEndOf="parent"
            app:layout_constraintTop_toTopOf="parent"
            />

        <View
            android:id="@+id/footerView"
            android:layout_width="match_parent"
            android:layout_height="1px"
            android:background="#E6E6E6"
            app:layout_constraintBottom_toBottomOf="parent"
            />
    </androidx.constraintlayout.widget.ConstraintLayout>
</layout>
